apiVersion: v1
kind: Service
metadata:
  name: {{ include "stratt.fullname" . }}
  labels: {{- include "stratt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "stratt.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
