# kind cluster for dispatcher development (ADR-0007). Vanilla upstream
# Kubernetes — nothing in the dispatcher may assume kind (§7.1).
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: stratt-dev
nodes:
  - role: control-plane
