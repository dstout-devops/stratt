# Tomcat on the svc tier — the SECOND application, and the proof is that this is all it took.
#
# No new Workflow: tomcat-configure already ships and, since converge Workflows inherit their View,
# it targets whatever an Assignment binds. No new Actuator, no new content root, no change to the
# apache tier. One Intent for the hosts, one View, one Assignment.
name: svc-servers-tomcat
intent: tomcat-tier@1
view: svc-servers
blueprint: tomcat@1
environments: [dev]
ackDelta: 1
