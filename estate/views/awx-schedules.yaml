# The AWX-estate audit View over the ansible.schedule mirror the awx Connector projects
# (§1.2 read-only). The governance capability + operators query the AWX estate through
# these — the app-of-apps-over-AWX surface, consuming the ansible.schedule projection.
name: awx-schedules
selector:
  kinds: [ansible.schedule]
