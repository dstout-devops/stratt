# The AWX-estate audit View over the ansible.template mirror the awx Connector projects
# (§1.2 read-only). The governance capability + operators query the AWX estate through
# these — the app-of-apps-over-AWX surface, consuming the ansible.template projection.
name: awx-templates
selector:
  kinds: [ansible.template]
