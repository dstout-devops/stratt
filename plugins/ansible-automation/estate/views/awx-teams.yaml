# The AWX-estate audit View over the ansible.team mirror the awx Connector projects
# (§1.2 read-only). The governance capability + operators query the AWX estate through
# these — the app-of-apps-over-AWX surface, consuming the ansible.team projection.
name: awx-teams
selector:
  kinds: [ansible.team]
