# Generated by `stratt import awx` (ADR-0025). Review before apply.
name: awx/smart-web
selector:
    labels:
        awx.group.name: prod
    facets:
        - namespace: ansible_distribution
          path: family
          equals: RedHat
