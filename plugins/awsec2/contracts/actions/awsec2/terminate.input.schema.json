{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stratt:contract:actions/awsec2/terminate.input:v1",
  "title": "awsec2/terminate input",
  "description": "Input Contract for the awsec2/terminate lifecycle Action (ADR-0095). The instance to act on, by native id. AWS credentials inject as a CredentialRef (§2.5), not params.",
  "type": "object",
  "properties": {
    "instanceId": { "type": "string", "description": "the aws.instanceId of the target instance" }
  },
  "required": ["instanceId"],
  "additionalProperties": false
}
