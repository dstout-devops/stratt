{ "$schema": "https://json-schema.org/draft/2020-12/schema", "$id": "stratt:contract:actions/awss3/delete-bucket.input:v1", "title": "awss3/delete-bucket input",
  "description": "Delete an S3 bucket (ADR-0097). REFUSED for protected buckets (the Evidence WORM store, ADR-0029). AWS credentials inject as a CredentialRef (§2.5).",
  "type": "object", "properties": { "name": {"type":"string"} }, "required": ["name"], "additionalProperties": false }
