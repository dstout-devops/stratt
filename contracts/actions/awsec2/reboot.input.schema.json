{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stratt:contract:actions/awsec2/reboot.input:v1",
  "title": "awsec2/reboot input",
  "description": "Input Contract for the awsec2/reboot lifecycle Action (ADR-0095). The instance to act on, by native id. AWS credentials inject as a CredentialRef (§2.5), not params.",
  "type": "object",
  "properties": {
    "instanceId": { "type": "string", "description": "the aws.instanceId of the target instance" }
  },
  "required": ["instanceId"],
  "additionalProperties": false
}
