{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "stratt:contract:actions/awsec2/create-vm.output:v1",
  "title": "awsec2/create-vm output",
  "description": "Output Contract: the provisioned instance's identity — bindable into a downstream configure Step ({{.steps.<name>.outputs.instanceId}}).",
  "type": "object",
  "properties": {
    "instanceId": { "type": "string" },
    "privateIp": { "type": "string" }
  },
  "required": ["instanceId"],
  "additionalProperties": false
}
