# CIS 5.2.16 SSH idle timeout — asserted graph-side against the os.hardening.sshd Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-5-2-16-alive-interval
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: info
framework: cis
expected:
  - namespace: os.hardening.sshd
    path: client_alive_interval
    equals: "300"
