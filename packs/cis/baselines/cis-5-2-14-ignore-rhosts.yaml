# CIS 5.2.14 SSH IgnoreRhosts — asserted graph-side against the os.hardening.sshd Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-5-2-14-ignore-rhosts
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: warning
framework: cis
expected:
  - namespace: os.hardening.sshd
    path: ignore_rhosts
    equals: "yes"
