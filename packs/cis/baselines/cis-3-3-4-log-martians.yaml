# CIS 3.3.4 log martian packets — asserted graph-side against the os.hardening.sysctl Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-3-3-4-log-martians
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: info
framework: cis
expected:
  - namespace: os.hardening.sysctl
    path: all_log_martians
    equals: "1"
