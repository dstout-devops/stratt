# CIS 5.2.6 disable X11 forwarding — asserted graph-side against the os.hardening.sshd Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-5-2-6-x11-forwarding
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: info
framework: cis
expected:
  - namespace: os.hardening.sshd
    path: x11_forwarding
    equals: "no"
