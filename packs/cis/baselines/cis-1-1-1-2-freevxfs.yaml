# CIS 1.1.1.2 disable freevxfs — asserted graph-side against the os.hardening.filesystem Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-1-1-1-2-freevxfs
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: info
framework: cis
expected:
  - namespace: os.hardening.filesystem
    path: freevxfs_disabled
    equals: true
