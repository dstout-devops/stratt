# CIS 4.1.1.2 auditd running — asserted graph-side against the os.hardening.auditd Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-4-1-1-2-auditd-running
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: warning
framework: cis
expected:
  - namespace: os.hardening.auditd
    path: running
    equals: true
