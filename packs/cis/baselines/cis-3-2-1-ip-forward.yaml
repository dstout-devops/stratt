# CIS 3.2.1 disable IP forwarding — asserted graph-side against the os.hardening.sysctl Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-3-2-1-ip-forward
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: warning
framework: cis
expected:
  - namespace: os.hardening.sysctl
    path: ip_forward
    equals: "0"
