# CIS 3.3.2 no source-routed packets — asserted graph-side against the os.hardening.sysctl Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-3-3-2-source-route
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: warning
framework: cis
expected:
  - namespace: os.hardening.sysctl
    path: all_accept_source_route
    equals: "0"
