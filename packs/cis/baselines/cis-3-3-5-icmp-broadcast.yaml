# CIS 3.3.5 ignore broadcast ICMP — asserted graph-side against the os.hardening.sysctl Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-3-3-5-icmp-broadcast
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: warning
framework: cis
expected:
  - namespace: os.hardening.sysctl
    path: icmp_echo_ignore_broadcasts
    equals: "1"
