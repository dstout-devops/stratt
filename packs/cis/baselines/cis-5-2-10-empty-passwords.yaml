# CIS 5.2.10 no empty SSH passwords — asserted graph-side against the os.hardening.sshd Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-5-2-10-empty-passwords
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: critical
framework: cis
expected:
  - namespace: os.hardening.sshd
    path: permit_empty_passwords
    equals: "no"
