# CIS 5.2.7 disable host-based auth — asserted graph-side against the os.hardening.sshd Facet the
# collector projects (ADR-0033). The benchmark is data: a control is a Baseline.
name: cis-5-2-7-hostbased-auth
viewName: ${VIEW}
mode: facet-observation
cron: "0 */6 * * *"
severity: warning
framework: cis
expected:
  - namespace: os.hardening.sshd
    path: hostbased_authentication
    equals: "no"
